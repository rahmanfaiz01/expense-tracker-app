# Frontend environment variables (copy to .env for local development).

# Base URL of the backend API, including the /api/v1 prefix.
# Production (Vercel): https://<your-railway-app>.up.railway.app/api/v1
VITE_API_URL=http://localhost:8000/api/v1
